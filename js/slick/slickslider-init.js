$(document).ready(function(){
  $('.content-slider1').slick({
    // autoplay: true;
    // autoplaySpeed: 3000;
    // centerMode: true;
    // centerPadding: '10px';
    // fade: true;
  });
});

$(document).ready(function(){
  $('.content-slider2').slick({
    // autoplay: true;
    // autoplaySpeed: 3000;
    // centerMode: true;
    // centerPadding: '10px';
    // fade: true;
  });
});
$(document).ready(function(){
  $('.jeneral-slider').slick({
    // autoplay: true;
    // autoplaySpeed: 3000;
    // centerMode: true;
    // centerPadding: '10px';
    // fade: true;
  });
});